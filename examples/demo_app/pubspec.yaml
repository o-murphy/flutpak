name: demo_app
description: A minimal Flutter hello-world app demonstrating flutpak Flatpak packaging.
version: 0.1.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  sqlite3: ^2.7.0
  sqlite3_flutter_libs: ^0.6.0
  rhttp: ^0.12.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
