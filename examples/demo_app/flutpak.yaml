output: flatpak

flutter:  # autoresolve to latest stable release

rust:
  version: 1.88.0
  rustup-path: /var/lib/rustup

foreign-deps-ref: fix-error-with-no-lock-file

repo-url: https://github.com/o-murphy/flutpak.git

app-id: io.github.o_murphy.flutpak.demo
runtime-version: '25.08'
command: demo_app
subdir: examples/demo_app

finish-args:
  - --share=network
