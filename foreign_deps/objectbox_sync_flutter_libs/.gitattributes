# Default: normalise text files to LF in the repo.
* text=auto eol=lf

# Patch files must be stored and checked out byte-for-byte unchanged.
# The 5.3.2 patch has CRLF context lines (the upstream CMakeLists.txt uses
# CRLF); git must not re-normalise them.
*.patch -text
