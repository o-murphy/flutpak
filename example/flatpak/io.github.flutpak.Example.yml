# Generated by flutpak — https://github.com/o-murphy/flutpak
#
# This is your editable TEMPLATE manifest — commit it to git.
# Run "flutpak generate" on every build to produce the final output in generated/.
#
# SAFE TO EDIT   finish-args, build-commands, build-options, sdk-extensions, modules
# AUTO-INJECTED  tag, commit, modules, manifest.sources, generated-sources.json, and patches are set/appended by generate
app-id: io.github.flutpak.Example
runtime: org.freedesktop.Platform
runtime-version: '24.08'
sdk: org.freedesktop.Sdk
command: Example
# Sandbox permissions — review and adjust for your app.
finish-args:
  - --share=ipc
  - --socket=fallback-x11
  - --socket=wayland
  - --device=dri
modules:
  - name: Example
    buildsystem: simple
    build-options:
      arch:
        x86_64:
          env:
            BUNDLE_PATH: build/linux/x64/release/bundle
        aarch64:
          env:
            BUNDLE_PATH: build/linux/arm64/release/bundle
      append-path: /run/build/Example/flutter/bin
      env:
        PUB_CACHE: /run/build/Example/.pub-cache
    # Build steps — review and customise for your project.
    build-commands:
      - cp flutter/bin/internal/engine.version flutter/bin/cache/engine-dart-sdk.stamp
      - cp flutter/bin/internal/material_fonts.version flutter/bin/cache/material_fonts.stamp
      - cp flutter/bin/internal/gradle_wrapper.version flutter/bin/cache/gradle_wrapper.stamp
      - cp flutter/bin/internal/engine.version flutter/bin/cache/engine_stamp.stamp
      - cp flutter/bin/internal/engine.version flutter/bin/cache/flutter_sdk.stamp
      - cp flutter/bin/internal/engine.version flutter/bin/cache/font-subset.stamp
      - cp flutter/bin/internal/engine.version flutter/bin/cache/linux-sdk.stamp
      - setup-flutter.sh
      - flutter build linux --release --no-pub
      - mkdir -p /app/Example
      - cp -r "$BUNDLE_PATH"/. /app/Example/
      - install -Dm755 flatpak/Example-wrapper.sh /app/bin/Example
      - install -Dm644 share/metainfo/io.github.flutpak.Example.metainfo.xml /app/share/metainfo/io.github.flutpak.Example.metainfo.xml
      - install -Dm644 share/applications/io.github.flutpak.Example.desktop /app/share/applications/io.github.flutpak.Example.desktop
    sources:
      - type: git
        url: https://github.com/o-murphy/flutpak
        disable-submodules: true
